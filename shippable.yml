language: python

build_image: htmlgraphic/postfix


before_script:
  - cd $PWD && curl -L "https://shunit2.googlecode.com/files/shunit2-2.1.6.tgz" | tar zx
  - echo $PWD
  - ls -la

script:
  - echo $PWD
  - cd $PWD
  - ls -la
  - . Docker/Postfix/build_tests.sh

cache: true
